<body>
  <div>
    <head>XPath Cheat Sheet</head>
    <div>
      <head>Syntax</head>
      
    </div>
    <div>
      <head>Axis Relations</head>
      <ul>
        <li>ancestor</li>
        <li>ancestor-or-self</li>
        <li>parent</li>
        <li>preceeding</li>
        <li>preceeding-sibling</li>
        <li>self</li>
        <li>following-sibling</li>
        <li>following</li>
        <li>child</li>
        <li>descendant-or-self</li>
        <li>descendant</li>
      </ul>
      <ul>
        <li>namespace</li>
        <li>attribute</li>
      </ul>
    </div>
    <div>
      <head>Functions</head>
      <p>XPath functions are also used in XQuery and XSLT! See the <ref target="http://www.w3schools.com/xpath/xpath_functions.asp">awesome and comprehensive list of functions</ref> at W3Schools for functions which didn't make it onto this list.</p>
      <ul>
        <li>not()</li>
        <li>name()</li>
        <li>local-name()</li>
        <li>empty()</li>
        <li>exists()</li>
        <li>count()</li>
        <li>distinct-values()</li>
        <li>position()</li>
        <ul>
          <lh>Strings</lh>
          <li>string()</li>
          <li>concat()</li>
          <li>string-join()</li>
          <li>normalize-space()</li>
          <li>contains()</li>
          <li>starts-with()</li>
          <li>ends-with()</li>
          <li>matches()</li>
          <li>replace()</li>
          <li>tokenize()</li>
        </ul>
      </ul>
    </div>
  </div>
  <div>
    <head>What Can You Do With XPath?</head>
    <p>As long as you know how to navigate the XML tree, you can characterize a document by its structural XML - its elements and attributes - as well as the human-friendly content, such as text values of those elements and attributes.</p>
    <p>With XPath, you can:</p>
    <ul>
      <li>find all values used in an attribute,</li>
      <li>identify all &lt;persName&gt;s that appear within &lt;person&gt;,</li>
      <li>return only those elements that meet a certain criteria, such as &lt;p&gt; only if it has a value of "nope" in @rend.</li>
    </ul>
    <p>Whereas XML schemas and documentation wikis will tell you how an element or attribute <emph>ought to be</emph> used, XPath can tell you how an element or attribute <emph>is</emph> used. It does not, however, make any judgments about the validity or correctness of its results. This makes it a useful tool for finding examples or errors, especially when used in conjunction with human judgment, XML schemas, documentation, encoding guidelines, and/or regular expressions.</p>
    <p>XPath can be used on either a document-level, or across many texts. Here we only test your XPath against one TEI-encoded document, but you can also define working sets within oXygen's XPath tool. Working sets can encompass multiple directories full of XML documents. <DEBUG>To set up a working set in oXygen</DEBUG></p>
  </div>
</body>